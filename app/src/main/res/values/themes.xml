<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme using Material3 DayNight -->
    <style name="Base.Theme.Moviesta" parent="Theme.Material3.DayNight">
        <!-- Disable the old ActionBar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize colors, typography, etc. here if needed -->
    </style>

    <!-- Main app theme inheriting from base -->
    <style name="Theme.Moviesta" parent="Base.Theme.Moviesta" />

</resources>
